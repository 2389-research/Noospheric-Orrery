{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main and logs windows",
  "windows": ["main", "logs"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
