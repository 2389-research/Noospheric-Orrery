# ABOUTME: Build and push Docker images to GHCR on every push to main and on releases.
# ABOUTME: Builds orchestrator, worker, and frontend images in parallel via matrix strategy.

name: Release Docker Images

on:
  release:
    types: [published]
  push:
    branches: [main]

env:
  REGISTRY: ghcr.io

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    strategy:
      matrix:
        include:
          - service: orchestrator
            image: orrery-orchestrator
            dockerfile: orchestrator/Dockerfile.local
            context: .
          - service: worker
            image: orrery-worker
            dockerfile: worker/Dockerfile
            context: .
          - service: frontend
            image: orrery-frontend
            dockerfile: frontend/Dockerfile
            context: ./frontend
            build-args: |
              NEXT_PUBLIC_AUTH_MODE=noop
              BACKEND_URL=http://orchestrator:8000

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata (tags, labels)
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ github.repository_owner }}/${{ matrix.image }}
          tags: |
            # semver tag from GitHub releases (e.g. v0.4.0 -> :0.4.0)
            type=semver,pattern={{version}}
            # :latest from every main push + every release
            type=raw,value=latest
            # short SHA tag so users can pin to a specific commit
            type=sha,format=short

      - name: Build and push image
        uses: docker/build-push-action@v6
        with:
          context: ${{ matrix.context }}
          file: ${{ matrix.dockerfile }}
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          build-args: ${{ matrix.build-args }}
